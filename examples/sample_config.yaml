# batchmark sample configuration
# Run with: batchmark run examples/sample_config.yaml

timeout: 10
repeat: 3

commands:
  - echo "hello world"
  - ls /tmp
  - python3 -c "import time; time.sleep(0.05)"
  - date
  - uname -a
  - python3 -c "print('batchmark')"
  - cat /etc/hostname
  - pwd
